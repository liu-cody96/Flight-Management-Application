import styled from "styled-components";
import { Link } from "react-router-dom";

export const FlightUpdateButton = styled(Link)`
    color: inherit; /* Use the color that the parent element is using */
    text-decoration: none;

`;
